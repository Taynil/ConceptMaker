@model IEnumerable<ConceptMaker.Models.Component>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Stworz nowe", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.ComponentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Concept.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SubConcept.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Required)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <th>
            @Html.DisplayFor(modelItem => item.ComponentName)
        </th>
        <td>
            @Html.DisplayFor(modelItem => item.Concept.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SubConcept.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Required)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Detale", "Details", new { id = item.Id }) |
            @Html.ActionLink("Usuń", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
